services:
  db:
    container_name: postgres_db
    image: postgres:latest
    restart: always
    environment:
     POSTGRES_USER: ${POSTGRESQL_USER}
     POSTGRES_PASSWORD: ${POSTGRESQL_PASSWORD}
     POSTGRES_DB: postgres_db
     TZ: "Asia/Seoul"
    ports:
      - "5432:5432"
    volumes:
      - ./docker/postgres/data:/var/lib/postgresql/data

  mongodb:
    container_name: mongodb
    image: mongo:latest
    restart: always
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_INITDB_ROOT_USERNAME}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_INITDB_ROOT_PASSWORD}
      MONGO_INITDB_DATABASE: mongo_db
      TZ: "Asia/Seoul"
    ports:
      - "27017:27017"
    volumes:
      - ./docker/mongodb/data/db:/data/db
      - ./docker/mongodb/init-mongo.js:/docker-entrypoint-initdb.d/init-mongo.js


#    volumes:
#      - ./docker/rpakadb/mysql_data:/var/lib/mysql
#      - ./docker/rpakadb/my.cnf:/etc/mysql/my.cnf
#  nginx:
#    image: nginx:latest
#    container_name: nginx-rpaka
#    ports:
#      - "80:80"
#      - "443:443"
#    volumes:
#      - ./docker/nginx/nginx.conf:/etc/nginx/nginx.conf:ro
#      - ./docker/nginx/certs:/etc/ssl/certs:ro
#      - ./docker/nginx/private:/etc/ssl/private:ro5
#      - ./docker/nginx/static:/usr/share/nginx/html
#
#networks:
#  host:
#    external: true

